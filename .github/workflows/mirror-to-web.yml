# DEPRECATED: dns-tool-web is archived. Single-repo migration complete 2026-03-30.
# This workflow is disabled. See docs/SINGLE_REPO_MIGRATION.md.
name: Mirror to dns-tool-web (OSS) [DEPRECATED]

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Mirror deprecated
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated. dns-tool-web has been archived."
          echo "All development now happens in dns-tool-intel (single canonical repo)."
          echo "See docs/SINGLE_REPO_MIGRATION.md for details."
