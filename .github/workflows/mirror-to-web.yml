name: Mirror to dns-tool-web (OSS)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mirror:
    name: Sync filtered mirror
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "Mirror workflow is alive"
